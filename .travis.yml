language: python

python:
  - 3.6
  - 3.7
  - 3.8

install:
  - pip install .
  - pip install pytest pytest-cov codecov

script:
  - pytest -v --cov-config=.coveragerc --cov=./PETScGMsFEM

after_success:
  - codecov
